%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% THE UNIFIED CEREBRUM DYNAMICAL SYSTEM
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% 1. GLOBAL VOLUMETRIC GRADIENT (Neuromodulation via Prediction Error Statistics)
% E = U_pred - U_target (where U is the sensory/projection stream)
\nu_{t+1} = (1 - \delta)\nu_t + \delta \cdot \tanh(k_{chem} \cdot \text{Metric}(\mathbf{E}))

% Metric Mapping:
% DA_c  (Dopamine)       ∝ mean(E)    -> Reward Prediction Error (Bias/Plasticity)
% NE_c  (Norepinephrine) ∝ mean(|E|)  -> Saliency/Arousal (Gain/Arousal)
% ACh_c (Acetylcholine)  ∝ mean(E^2)  -> Uncertainty (Signal/Recurrence Ratio)

% 2. BASAL GANGLIA GATING (Dual-Path Selection & Contrast Enhancement)
% Raw Go/NoGo Integration
G_{raw} = \kappa \cdot [ \sigma((1+\alpha DA_c) \cdot s) - \sigma(-(1+\beta DA_c) \cdot s) ]

% Z-Score Normalization (Contrast Enhancement Loop)
z_i = \frac{G_{i} - \mu_G}{\sigma_G + \epsilon} \implies g_i = \exp(k_{contrast} \cdot z_i)

% 3. THALAMIC DSB RELAY (Hierarchical Information Relay)
% B_t = Neuromodulated sum of Bottom-Up drive (H_prev) and Top-Down expectation (U_t)
z_{t+1} = \text{ReLU}\left((1-\lambda_{decay})z_t + B_t\right) \cdot g_t
E_t^{(l)} = W_{tc} z_{t+1}

% 4. CORTICAL DYANMICS (Meso-scale Recurrence with Lateral Inhibition)
\Psi^{(l)} = \frac{f_{\text{softlog}}\left((1 - ACh_c) (\Phi_{rec}(H_t^{(l)}) + E_t^{(l)})\right) - \sigma_{sub}}{\sigma_{div}}

% Meso-scale Kernels (Spatial competition):
% sigma_sub = local subtractive kernel (r=3)
% sigma_div = local divisive gain kernel (r=7)

% 5. MULTI-SCALE PLASTICITY (Neuromodulated Oja Rules)
% Cortex (Real-time Low-Rank Associative Manifold Discovery)
\Delta A = \eta_{gate} [ \mathcal{E} \cdot H^T B - H(H^T A)(B^T B) ] - \lambda A

% Basal Ganglia (Striatal Synaptic Competition / Action-Value Mapping)
\Delta W_{str} = \eta_{DA} \cdot (g H^T - g^2 W_{str})

% 6. MOTOR GAIN MODULATION (Optra)
\text{Action}_t = 1 + \beta \cdot \tanh(\alpha \cdot \text{Action}_{raw})